<!DOCTYPE html>
<html>
  <head>
    <title><%= @page_title%></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!search engine optimization-->
    <META name="keywords" content="<%=@seo_keywords%>" />

    <! see stylesheets folder as this is what the 'blogs' link refers to-->
    <%= stylesheet_link_tag    'blogs', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    
    <!showing user and login info-->
    <% if current_user.is_a?(User)%>
   
    <!changing route to Logout rather than sign out-->
    <%= link_to "Logout", destroy_user_session_path, method: :delete %>

    <% else%>
     <%= link_to "Register", new_user_registration_path%>
      <%= link_to "Login", new_user_session_path%>
    <% end%>

    <! code from any other page or file will slide into the yield method, allowing it to appear alongside the code being used on the entire application, it dynamically slides in code-->
    <%= yield %>

    <% if session[:source]%>
   <p>Thanks for visiting me from <%= session[:source] %></p> 
    <% end %>

    <div> <!application layout page allows us to only use this code on page but let the links appear on every page-->
      <div><%= link_to "Home", root_path %></div>
      <div><%= link_to "About", about_path %></div>
      <div><%= link_to "Contact", contact_path %></div>
       <div><%= link_to "Blogs", blogs_path %></div>
        <div><%= link_to "Portfolio", portfolios_path %></div>
    </div>

  </body>
</html>
